---
layout: layout.njk
title: Galerie
---

<h2 class="text-3xl font-semibold mb-6 text-center">Galerie</h2>

<div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
  {% for image in collections.gallery %}
  <div class="overflow-hidden rounded-lg shadow-lg hover:shadow-xl transition duration-300 bg-white">
    <img
      src="{{ image.url }}"
      alt="{{ image.alt }}"
      class="w-full h-48 object-cover transform hover:scale-105 transition duration-300"
      loading="lazy"
    />
    <div class="p-4">
      <h3 class="font-semibold text-lg mb-1">{{ image.title }}</h3>
      <p class="text-sm text-gray-600">{{ image.description }}</p>
    </div>
  </div>
  {% else %}
  <p class="col-span-full text-center text-gray-500">Keine Bilder vorhanden.</p>
  {% endfor %}
</div>

